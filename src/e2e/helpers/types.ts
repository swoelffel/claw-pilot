// src/e2e/helpers/types.ts
// Shared type aliases for E2E tests — centralizes the single `any` bypass.

/** Parsed JSON body — intentionally loose for E2E test assertions. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Json = any;
