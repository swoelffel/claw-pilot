# TOOLS.md — {{agentName}}

## Tool profile: coding

Standard tools from the **coding** profile are enabled:

| Tool | Description |
|------|-------------|
| `read` | Read file contents |
| `write` | Write or overwrite a file |
| `edit` | Apply targeted edits to a file |
| `exec` | Execute shell commands |
| `glob` | Find files by pattern |
| `grep` | Search file contents by regex |
| `webFetch` | Fetch content from a URL |

## Agent-to-agent communication

### Teammates

{{#each agents}}
- `{{this.id}}` — {{this.name}}
{{/each}}

### Syntax

```
agentToAgent(agentId: "<target-id>", message: "<your message>", timeoutSeconds: 180)
```

### Timeout policy

| Duration | Use case |
|----------|----------|
| 60s | Quick question or clarification |
| 120s | Short task, file update |
| **180s** | **Standard task (default)** |
| 300s | Long document, complex implementation |

> Never use the default timeout (30s) — it is too short for most tasks.

### Control tokens

- `REPLY_SKIP` — End the exchange when there is nothing more to add.
- `ANNOUNCE_SKIP` — Suppress the summary post to the external channel.

## MCP servers

If MCP servers are configured for this instance, their tools will appear
automatically. Check the instance configuration for available MCP integrations.

*Generated by claw-pilot on {{date}}*
