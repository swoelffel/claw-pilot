# Copy to .env.local and fill in values
# NEVER commit .env.local

# LLM providers — at least one required to test claw-runtime
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional: override dashboard port (default: 19000)
# CLAW_PILOT_DASHBOARD_PORT=19000
